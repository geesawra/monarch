engine: nixery

when:
  - event: ["push", "pull_request"]
    branch: ["main"]

dependencies:
  nixpkgs:
    - fastlane
    - jdk24

steps:
  - name: build-release-apk
    command: |
      echo "Here we are"
      fastlane release_bin
